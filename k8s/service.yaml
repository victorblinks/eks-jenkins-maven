apiVersion: v1
kind: Service
metadata:
  name: regapp-service
spec:
  type: NodePort
  selector:
    app: regapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080